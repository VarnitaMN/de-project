#include<stdio.h>
#include<string.h>

int main(){
    int n=4,j,i;
    for(i=1;i<=n;i++)
{
    for(j=1;j<=n;j++)
    printf(" ");
      for(j=1;j<=n;j++)
    {
        if(i==1||j==1||i==n||j==n)
        printf("* ");
        else 
        printf("  ");

    }
printf("\n");
}
}
